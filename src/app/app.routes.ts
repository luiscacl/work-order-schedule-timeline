import { Routes } from '@angular/router';
import { WorkOrderSchedulePage } from './work-order-schedule/work-order-schedule.page';

export const routes: Routes = [
  { path: '', component: WorkOrderSchedulePage },
];